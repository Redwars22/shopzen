import { Button, View } from "react-native";
import NoUserComponent from "../../components/NoUser/NoUser.component";

export default function AccountScreen({navigation}){
    return(
        <View>
            <NoUserComponent/>
        </View>
    );
}